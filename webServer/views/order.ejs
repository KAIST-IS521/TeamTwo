<!DOCTYPE html>
<html lang="en">
  <head>
    <title> Cart Page: Flag are us </title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css?family=Lato" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Montserrat" rel="stylesheet" type="text/css">
    <link rel="stylesheet" type="text/css" href="css/order.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <script src="js/order.js"></script>
  </head>
  <body>
    <nav class="navbar navbar-default navbar-fixed-top">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#myPage">Flagrus</a>
        </div>
        <div class="collapse navbar-collapse" id="myNavbar">
          <ul class="nav navbar-nav navbar-right">
            <li><a href="#">WELCOME <%= user %></a></li>
            <li><a href="main.html">HOME</a></li>
            <li><a href="main.html#product">PRODUCT</a></li>
            <li><a href="main.html#contact">CONTACT</a></li>
            <li><a href="cart.html">CART</a></li>
            <li><a href="order.html">ORDERS</a></li>
            <li><a href="mypage.html">MYPAGE</a></li>
            <li><a href="login.html">LOGIN</a></li>
            <li><a href="logout.html">LOGOUT</a></li>
            <li><a href="search.html"><span class="glyphicon glyphicon-search"></span></a></li>
          </ul>
        </div> <!-- end of menues -->
      </div>
    </nav>

    <div id="orderMain" class="bg-1">
      <div class="container">
        <h3 class="text-center"> Orders </h3>
        <p class="text-center"> Check your orders :/ </p>

        <% orders.forEach(order, index) { %>
        <% if (orders.length != 0) { %>
        <% if (index == 0 || ordres[index].order_id != orders[index-1].order_id) { %>
        <% var total = 0; %>
        <% if (order.status == 'pending') { %>
        <div class="panel panel-primary order-item">
        <% } else if (order.status == 'completed') { %>
        <div class="panel panel-success order-item">
        <% } else { %>
        <div class="panel panel-danger order-item">
        <% } %>

          <div class="panel-heading order-item-head">
            Order number #<%= order.order_id %>
            <div class="account-number"> Account: <%= order.bank_account %> </div>
          </div>
          <div class="panel-body">

            <div class="order-status">
              <%= order.status %>
            </div>

            <div class="list-group order-item-list">
              <% } %>

              <div class="list-group-item">
                <%= order.name %>
                <div class="id-store"><%= order.product_id %></div>
                <% if (order.status == 'completed') { %>
                <button type="button" class="btn btn-secondary"> view </button>
                <% } %>
                <span class="badge"> <%= order.num %> </span>
              </div>
              <% total += order.price * order.num %>

        <% if (index == order.length-1 || ordres[index].order_id != orders[index+1].order_id) { %>
              <strong> Order time : <%= order.time %> </strong>
              </br>
              <strong> Total : <%= total %> won </strong>
              </br>
              <strong> <%= order.status %> </strong>
            </div>
            <% } %>

          </div>
        </div>
        <% } %>
        <% } %>

        <!--
        <div class="panel panel-primary order-item">
          <div class="panel-heading order-item-head">
            Order number #1
            <div class="account-number"> Account: TempAccount1 </div>
          </div>
          <div class="panel-body">

            <div class="order-status">
              pending...
            </div>

            <div class="list-group order-item-list">
              <div class="list-group-item">
                Planet craft
                <div class="id-store">1</div>
                <button type="button" class="btn btn-secondary"> view </button>
                <span class="badge"> 2 </span>
              </div>
              <div class="list-group-item">
                Planet craft 2
                <button type="button" class="btn btn-secondary"> view </button>
                <span class="badge"> 3 </span>
              </div>
              <div class="list-group-item">
                Soccer manager 2022
                <button type="button" class="btn btn-secondary"> view </button>
                <span class="badge"> 4 </span>
              </div>
              <div class="list-group-item">
                Flag
                <button type="button" class="btn btn-secondary"> view </button>
                <span class="badge"> 5 </span>
              </div>
              <strong> Order time : xx-xx-xx </strong>
              </br>
              <strong> Total : 100 won </strong>
              </br>
              <strong> pending... </strong>
            </div>

          </div>
        </div>
        -->

      </div> <!-- end of container -->
    </div> <!-- end of order Main -->

  </body>
</html>
